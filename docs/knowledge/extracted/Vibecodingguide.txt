🛡️ Synapse Sentinel (v24)The Universal Governance Engine for SynapseIndia Engineering.Synapse Sentinel is a compiled, tamper-proof governance binary that automates the setup, security, and maintenance of AI-augmented software projects. It enforces Agency Standards across Web, Mobile, Commerce, and AI service lines without exposing proprietary prompt engineering to the source code.🚀 Getting Started1. InstallationThe Sentinel is a single binary executable.Admins/Leads: Run the builder script (synapsevibsentinel.sh) to compile the binary from source.Developers: Place the sentinel file in the root of your project.2. Initialization (Bootstrap)Run the initialization command to configure the project structure and inject AI rules../sentinel initYou will be prompted to select your project DNA:Service Line: (Web, Cross-Platform Mobile, Native Mobile, Commerce, or AI).Database: (SQL, NoSQL, or None).Protocol: (Support for Legacy SOAP/WSDL).The Sentinel will automatically generate the .cursor/rules, .github/workflows, and docs/ folders based on your choices.🛠️ Operational WorkflowA. The "Vibe" Coding LoopOnce initialized, open Cursor or Windsurf. The AI is now pre-loaded with hidden instructions (The Constitution & Firewall).Context: The AI automatically reads docs/knowledge/client-brief.md.Prompting:❌ "Fix the bug" -> Rejected (Vague).❌ "Delete the users table" -> Rejected (Destructive).✅ "Refactor auth.ts to use Zod validation as per Web Standards." -> Accepted.B. The Security Gate (audit)You cannot push code without passing the Sentinel Audit. This runs automatically on git commit, but you can run it manually:./sentinel auditWhat it checks: | Check | Severity | Description | | :--- | :--- | :--- | | Secrets | 🔴 CRITICAL | Scans for high-entropy strings (API keys, tokens). | | Debug | ⚠️ WARNING | Scans for console.log or print statements. | | Database | 🔴 CRITICAL | Scans for NOLOCK (MSSQL) or $where (Mongo Injection). | | SOAP | ⚠️ WARNING | Scans for simplexml_load_string (XXE Vulnerability). |C. The Auto-Scribe (docs)Never write file maps manually. Update the AI's context window with the current project structure:./sentinel docsUpdates docs/knowledge/file-structure.txt.D. Legacy Migration (refactor)If working on a Brownfield project, use the safe refactoring protocol:./sentinel refactorInitiates the "Pin & Prune" workflow to safely upgrade legacy code without breaking functionality.🔐 Security ArchitectureThe "Black Box" ModelUnlike shell scripts, the Sentinel is a compiled Go binary.Hidden IP: The prompt engineering ("The Constitution") is embedded inside the binary. It is not visible in plain text scripts.Tamper Proof: The audit logic (runAudit) is machine code. Developers cannot "comment out" security checks.Ephemeral Rules: The AI rules are written to .cursor/rules but are immediately added to .gitignore. They exist only on the developer's machine, never in the remote repo.📦 Supported Stacks (Matrix)Sentinel v24 supports the full SynapseIndia portfolio:CategoryTechnologyEnforced StandardsWebReact, Next.js, Node, .NETModular Monolith, Zod ValidationMobile (Native)Swift, KotlinSwiftUI, Jetpack Compose, MVVMMobile (Cross)Flutter, React NativeBridge Safety, 3x AssetsCommerceMagento, Shopify, WooNo Global Scope, DI patternsAI / DataPython, PyTorchSeed Reproducibility, No Notebook SecretsDatabaseSQL, NoSQLMigration Safety, Indexing EnforcementProtocolSOAP, RESTXXE Protection, Typed Clients🚨 Troubleshooting"Permission Denied"Run chmod +x sentinel"AI is ignoring my rules"Run ./sentinel init again to re-inject the latest ruleset."Commit failed"Read the Sentinel output. It likely found a Secret or a banned pattern (e.g., NOLOCK). Fix the code and try again.Property of Synapse Engineering Strategy.