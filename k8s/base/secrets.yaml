apiVersion: v1
kind: Secret
metadata:
  name: sentinel-hub-api-secrets
  labels:
    app: sentinel-hub-api
    app.kubernetes.io/name: sentinel-hub-api
    app.kubernetes.io/part-of: sentinel
type: Opaque
stringData:
  # NOTE: In production, use sealed-secrets, external-secrets, or similar
  # These are placeholder values - replace with actual secrets
  database-url: "postgresql://user:password@postgres:5432/sentinel?sslmode=disable"
  jwt-secret: "change-me-in-production"
