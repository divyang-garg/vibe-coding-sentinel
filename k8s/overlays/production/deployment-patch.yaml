apiVersion: apps/v1
kind: Deployment
metadata:
  name: sentinel-hub-api
spec:
  template:
    spec:
      containers:
      - name: sentinel-hub-api
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
