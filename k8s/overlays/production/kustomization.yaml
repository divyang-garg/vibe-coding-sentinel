apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: sentinel-production

resources:
- ../../base

replicas:
- name: sentinel-hub-api
  count: 3

patchesStrategicMerge:
- deployment-patch.yaml
- configmap-patch.yaml

images:
- name: sentinel-hub-api
  newName: registry.example.com/sentinel-hub-api
  newTag: v1.0.0
