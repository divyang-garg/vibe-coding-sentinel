# Sentinel Hub API - Production Environment Configuration
# Complies with CODING_STANDARDS.md: Production security standards

# Server Configuration
HOST=0.0.0.0
PORT=8080
READ_TIMEOUT=30s
WRITE_TIMEOUT=30s
IDLE_TIMEOUT=120s

# Database Configuration (set via Docker secrets or environment)
DATABASE_URL=postgres://sentinel:${POSTGRES_PASSWORD}@postgres:5432/sentinel?sslmode=require

# Security Configuration (set via Docker secrets)
JWT_SECRET=${JWT_SECRET}
JWT_EXPIRATION=24h
BCRYPT_COST=12

# Rate Limiting (stricter in production)
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=15m

# CORS Configuration (restrictive in production)
CORS_ALLOWED_ORIGINS=${CORS_ALLOWED_ORIGINS}

# LLM Configuration
OLLAMA_HOST=${OLLAMA_HOST}
AZURE_AI_ENDPOINT=${AZURE_AI_ENDPOINT}
AZURE_AI_KEY=${AZURE_AI_KEY}
AZURE_AI_DEPLOYMENT=${AZURE_AI_DEPLOYMENT}
AZURE_AI_API_VERSION=${AZURE_AI_API_VERSION}
LLM_REQUEST_TIMEOUT=120s
LLM_MAX_RETRIES=5
LLM_RATE_LIMIT_PER_MINUTE=120

# Logging (structured logging in production)
LOG_LEVEL=info

# Production-specific settings
DEBUG=false
HOT_RELOAD=false
ENVIRONMENT=production